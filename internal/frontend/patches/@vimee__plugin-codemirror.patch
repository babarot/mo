diff --git a/dist/index.cjs b/dist/index.cjs
index 8ebd468f36519c154458d353415230265c7eb525..74e023452dd61e39f122c49eba9d94f0c2289e27 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -137,6 +137,7 @@ function attach(view, options = {}) {
   });
   let isComposing = false;
   let prevSearchHighlight = "";
+  let lastInsertState = null;
   const cmDispatch = view.dispatch.bind(view);
   cmDispatch({
     effects: import_state.StateEffect.appendConfig.of([searchHighlightField, searchHighlightTheme])
@@ -252,6 +253,7 @@ function attach(view, options = {}) {
     };
   }
   function shouldPreventDefault(e) {
+    if (e.metaKey) return false;
     const mode = ctx.mode;
     if (mode !== "insert") {
       if (e.ctrlKey && (e.key === "c" || e.key === "a")) return false;
@@ -282,9 +284,28 @@ function attach(view, options = {}) {
     ctx = { ...ctx, cursor: newCursor };
     syncCursorToEditor(newCursor);
   }
+  function syncBufferFromEditor() {
+    const editorContent = view.state.doc.toString();
+    if (editorContent !== buffer.getContent()) {
+      buffer.replaceContent(editorContent);
+    }
+  }
+  function syncCursorFromEditor() {
+    if (ctx.mode === "visual" || ctx.mode === "visual-line" || ctx.mode === "visual-block") {
+      return;
+    }
+    const offset = view.state.selection.main.head;
+    const editorCursor = offsetToCursor(buffer.getContent(), offset);
+    if (editorCursor.line !== ctx.cursor.line || editorCursor.col !== ctx.cursor.col) {
+      ctx = { ...ctx, cursor: editorCursor };
+    }
+  }
   function onKeyDown(e) {
     if (isComposing) return;
+    syncBufferFromEditor();
+    syncCursorFromEditor();
     syncViewport();
+    if (e.metaKey) return;
     if (e.ctrlKey) {
       const scrollKeys = {
         b: { direction: "up", amount: 1 },
@@ -304,6 +325,11 @@ function attach(view, options = {}) {
       e.preventDefault();
       e.stopPropagation();
     }
+    if (ctx.mode === "insert" && e.key.length === 1 && !e.ctrlKey && !e.metaKey) {
+      lastInsertState = { content: buffer.getContent(), cursor: { ...ctx.cursor } };
+    } else {
+      lastInsertState = null;
+    }
     const { newCtx, actions } = (0, import_core.processKeystroke)(e.key, ctx, buffer, e.ctrlKey, readOnly);
     ctx = newCtx;
     processActions(actions, newCtx, e.key);
@@ -315,12 +341,46 @@ function attach(view, options = {}) {
   }
   function onCompositionStart() {
     isComposing = true;
+    if (lastInsertState) {
+      buffer.replaceContent(lastInsertState.content);
+      ctx = { ...ctx, cursor: lastInsertState.cursor };
+      syncContentToEditor();
+      syncCursorToEditor(lastInsertState.cursor);
+      lastInsertState = null;
+    }
   }
   function onCompositionEnd() {
     isComposing = false;
+    syncBufferFromEditor();
+    syncCursorFromEditor();
+  }
+  function onMouseUp() {
+    const sel = view.state.selection.main;
+    if (sel.empty) {
+      if (ctx.mode === "visual" || ctx.mode === "visual-line" || ctx.mode === "visual-block") {
+        ctx = { ...ctx, mode: "normal", visualAnchor: null };
+        onModeChange?.("normal");
+        onAction?.({ type: "mode-change", mode: "normal" });
+      }
+      syncCursorFromEditor();
+      onAction?.({ type: "noop" });
+      return;
+    }
+    const content2 = buffer.getContent();
+    const anchorCursor = offsetToCursor(content2, sel.anchor);
+    const headCursor = offsetToCursor(content2, sel.head);
+    ctx = {
+      ...ctx,
+      mode: "visual",
+      visualAnchor: anchorCursor,
+      cursor: headCursor,
+    };
+    onModeChange?.("visual");
+    onAction?.({ type: "mode-change", mode: "visual" });
   }
   const target = view.contentDOM;
   target.addEventListener("keydown", onKeyDown, { capture: true });
+  target.addEventListener("mouseup", onMouseUp);
   target.addEventListener("compositionstart", onCompositionStart);
   target.addEventListener("compositionend", onCompositionEnd);
   syncViewport();
@@ -328,8 +388,10 @@ function attach(view, options = {}) {
     getMode: () => ctx.mode,
     getCursor: () => ({ ...ctx.cursor }),
     getContent: () => buffer.getContent(),
+    getCommandLine: () => ctx.commandType ? ctx.commandType + ctx.commandBuffer : "",
     destroy: () => {
       target.removeEventListener("keydown", onKeyDown, { capture: true });
+      target.removeEventListener("mouseup", onMouseUp);
       target.removeEventListener("compositionstart", onCompositionStart);
       target.removeEventListener("compositionend", onCompositionEnd);
     }
diff --git a/dist/index.js b/dist/index.js
index 0f433f473c0ba81b22bb6c59832b32a270d43fe2..eecb8c25738656ea956279d2c6b6e9e2e6bd5e93 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -107,6 +107,7 @@ function attach(view, options = {}) {
   });
   let isComposing = false;
   let prevSearchHighlight = "";
+  let lastInsertState = null;
   const cmDispatch = view.dispatch.bind(view);
   cmDispatch({
     effects: StateEffect.appendConfig.of([searchHighlightField, searchHighlightTheme])
@@ -222,6 +223,7 @@ function attach(view, options = {}) {
     };
   }
   function shouldPreventDefault(e) {
+    if (e.metaKey) return false;
     const mode = ctx.mode;
     if (mode !== "insert") {
       if (e.ctrlKey && (e.key === "c" || e.key === "a")) return false;
@@ -252,9 +254,28 @@ function attach(view, options = {}) {
     ctx = { ...ctx, cursor: newCursor };
     syncCursorToEditor(newCursor);
   }
+  function syncBufferFromEditor() {
+    const editorContent = view.state.doc.toString();
+    if (editorContent !== buffer.getContent()) {
+      buffer.replaceContent(editorContent);
+    }
+  }
+  function syncCursorFromEditor() {
+    if (ctx.mode === "visual" || ctx.mode === "visual-line" || ctx.mode === "visual-block") {
+      return;
+    }
+    const offset = view.state.selection.main.head;
+    const editorCursor = offsetToCursor(buffer.getContent(), offset);
+    if (editorCursor.line !== ctx.cursor.line || editorCursor.col !== ctx.cursor.col) {
+      ctx = { ...ctx, cursor: editorCursor };
+    }
+  }
   function onKeyDown(e) {
     if (isComposing) return;
+    syncBufferFromEditor();
+    syncCursorFromEditor();
     syncViewport();
+    if (e.metaKey) return;
     if (e.ctrlKey) {
       const scrollKeys = {
         b: { direction: "up", amount: 1 },
@@ -274,6 +295,11 @@ function attach(view, options = {}) {
       e.preventDefault();
       e.stopPropagation();
     }
+    if (ctx.mode === "insert" && e.key.length === 1 && !e.ctrlKey && !e.metaKey) {
+      lastInsertState = { content: buffer.getContent(), cursor: { ...ctx.cursor } };
+    } else {
+      lastInsertState = null;
+    }
     const { newCtx, actions } = processKeystroke(e.key, ctx, buffer, e.ctrlKey, readOnly);
     ctx = newCtx;
     processActions(actions, newCtx, e.key);
@@ -285,12 +311,46 @@ function attach(view, options = {}) {
   }
   function onCompositionStart() {
     isComposing = true;
+    if (lastInsertState) {
+      buffer.replaceContent(lastInsertState.content);
+      ctx = { ...ctx, cursor: lastInsertState.cursor };
+      syncContentToEditor();
+      syncCursorToEditor(lastInsertState.cursor);
+      lastInsertState = null;
+    }
   }
   function onCompositionEnd() {
     isComposing = false;
+    syncBufferFromEditor();
+    syncCursorFromEditor();
+  }
+  function onMouseUp() {
+    const sel = view.state.selection.main;
+    if (sel.empty) {
+      if (ctx.mode === "visual" || ctx.mode === "visual-line" || ctx.mode === "visual-block") {
+        ctx = { ...ctx, mode: "normal", visualAnchor: null };
+        onModeChange?.("normal");
+        onAction?.({ type: "mode-change", mode: "normal" });
+      }
+      syncCursorFromEditor();
+      onAction?.({ type: "noop" });
+      return;
+    }
+    const content2 = buffer.getContent();
+    const anchorCursor = offsetToCursor(content2, sel.anchor);
+    const headCursor = offsetToCursor(content2, sel.head);
+    ctx = {
+      ...ctx,
+      mode: "visual",
+      visualAnchor: anchorCursor,
+      cursor: headCursor,
+    };
+    onModeChange?.("visual");
+    onAction?.({ type: "mode-change", mode: "visual" });
   }
   const target = view.contentDOM;
   target.addEventListener("keydown", onKeyDown, { capture: true });
+  target.addEventListener("mouseup", onMouseUp);
   target.addEventListener("compositionstart", onCompositionStart);
   target.addEventListener("compositionend", onCompositionEnd);
   syncViewport();
@@ -298,8 +358,10 @@ function attach(view, options = {}) {
     getMode: () => ctx.mode,
     getCursor: () => ({ ...ctx.cursor }),
     getContent: () => buffer.getContent(),
+    getCommandLine: () => ctx.commandType ? ctx.commandType + ctx.commandBuffer : "",
     destroy: () => {
       target.removeEventListener("keydown", onKeyDown, { capture: true });
+      target.removeEventListener("mouseup", onMouseUp);
       target.removeEventListener("compositionstart", onCompositionStart);
       target.removeEventListener("compositionend", onCompositionEnd);
     }
